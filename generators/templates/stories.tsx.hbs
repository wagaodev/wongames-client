import { Story, Meta } from '@storybook/react';
import {{pascalCase name}} from '.';

export default {
  title: 'Components/{{pascalCase name}}',
  component: {{pascalCase name}},
} as Meta;

const Template: Story = () => <{{pascalCase name}} />;

export const Default = Template.bind({});
